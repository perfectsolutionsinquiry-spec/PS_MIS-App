{
  "name": "perfect-solutions-platform",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "build:api": "npm run build -w apps/api",
    "test": "npm run test -w apps/api"
  }
}
